---

docker_letsencrypt_path: /usr/local/letsencrypt
docker_letsencrypt_certs_path: "{{ docker_letsencrypt_path }}/certs"
docker_letsencrypt_nginx_tmpl_path: "{{ docker_letsencrypt_path }}/nginx.tmpl"
